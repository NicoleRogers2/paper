本章所研究的问题可形式化描述如下：给定一张由 HoloLens 2 PV 相机捕获的包含目标物体的真实 RGB 图像 I_ref∈R^(H×W×3)，以及该物体的先验三维网格模型 M，目标是估计物体相对于相机坐标系的六自由度位姿，即求解刚体变换（单独添加公式？）T=[R∣t]，其中 R∈SO(3) 为旋转矩阵，t∈R^3 为平移向量。相机内参矩阵K通过离线棋盘格标定获取，在后续优化过程中视为已知常量。
该问题的核心在于构建三维模型 M 与二维图像 I_ref之间的几何一致性约束。理想情况下，当估计的位姿准确时，三维模型在该位姿下的投影应与真实图像中物体的几何外观严格对齐（添加投影公式？）。然而，实际求解过程中存在以下挑战：1）渲染图像与真实图像之间存在明显的域差异（添加图片说明），主要表现为光照、材质纹理和背景环境的不一致，使得仅依赖像素级比较的优化方法易陷入局部最优；2）目标物体（如人体头部）几何形态较为平滑，缺乏显著的局部特征，导致基于轮廓或边缘的几何约束方法在匹配过程中存在歧义；3）位姿空间具有六维非线性流形结构（添加引用/说明？），优化过程对初始位姿高度敏感，需在收敛精度和算法鲁棒性之间取得平衡。
